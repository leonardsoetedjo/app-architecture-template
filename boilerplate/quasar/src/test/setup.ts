/// <reference types="vitest" />
import { config } from '@vue/test-utils'

// Global test setup for Quasar boilerplate
// Ensures proper environment for component + architecture tests

beforeAll(() => {
  // Stub Quasar plugins if needed
})

afterAll(() => {
  // Cleanup
})
