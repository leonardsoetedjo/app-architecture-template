package com.example.infrastructure.api;

import com.example.application.usecases.<%= featureNamePascal %>UseCase;
import com.example.application.dtos.<%= featureNamePascal %>Command;
import com.example.application.dtos.<%= featureNamePascal %>Result;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.autoconfigure.web.servlet.WebMvcTest;
import org.springframework.boot.test.mock.mockito.MockBean;
import org.springframework.http.MediaType;
import org.springframework.test.web.servlet.MockMvc;

import java.util.UUID;

import static org.mockito.ArgumentMatchers.any;
import static org.mockito.BDDMockito.*;
import static org.springframework.test.web.servlet.request.MockMvcRequestBuilders.post;
import static org.springframework.test.web.servlet.result.MockMvcResultMatchers.*;

/**
 * Integration tests for <%= entityName %>Controller.
 * Tests REST layer with mocked use case.
 */
@WebMvcTest(<%= entityName %>Controller.class)
@DisplayName("<%= entityName %>Controller Integration Tests")
class <%= entityName %>ControllerTest {

    @Autowired
    private MockMvc mockMvc;

    @MockBean
    private <%= featureNamePascal %>UseCase <%= featureNameCamel %>UseCase;

    @BeforeEach
    void setUp() {
        // Setup test data
    }

    @Test
    @DisplayName("Should handle <%= featureNamePascal %> request")
    void shouldHandleCreateRequest() throws Exception {
        // Given
        <%= featureNamePascal %>Command command = new <%= featureNamePascal %>Command();
        <%= featureNamePascal %>Result result = new <%= featureNamePascal %>Result(
            UUID.randomUUID(),
            java.time.Instant.now(),
            java.time.Instant.now()
        );
        
        given(<%= featureNameCamel %>UseCase.execute(any(<%= featureNamePascal %>Command.class)))
            .willReturn(result);

        // When and Then
        mockMvc.perform(post("/api/v1/<%= featureNameKebab %>")
                .contentType(MediaType.APPLICATION_JSON)
                .content("{}"))
            .andExpect(status().isCreated())
            .andExpect(header().exists("Location"));
    }
}
