package com.example.application.usecases;

import com.example.application.dtos.<%= featureNamePascal %>Command;
import com.example.application.dtos.<%= featureNamePascal %>Result;

/**
 * Use case interface for <%= featureNamePascal %>.
 * Defined in application layer - implementation alongside.
 */
public interface <%= featureNamePascal %>UseCase {

    /**
     * Execute <%= featureNamePascal %> use case.
     * @param command command containing input data
     * @return result containing output data
     */
    <%= featureNamePascal %>Result execute(<%= featureNamePascal %>Command command);
}
