package com.example.application.dtos;

import java.util.UUID;

/**
 * Command DTO for <%= featureNamePascal %>.
 * Contains input data for the use case.
 */
public record <%= featureNamePascal %>Command(
    // Add command fields here
    // Example:
    // UUID id,
    // String name,
    // BigDecimal amount
) {
    public <%= featureNamePascal %>Command {
        // Add validation here
        // Example:
        // if (name == null || name.isBlank()) {
        //     throw new IllegalArgumentException("Name is required");
        // }
    }

    public static <%= featureNamePascal %>Command create(/* params */) {
        return new <%= featureNamePascal %>Command(/* args */);
    }
}
