"""
Use case for <%= featureNamePascal %>.
Application layer business logic.
"""
from dataclasses import dataclass
from typing import Protocol

from src.application.dtos.<%= featureNameSnake %>_dtos import (
    <%= featureNamePascal %>Command,
    <%= featureNamePascal %>Result,
)


class <%= featureNamePascal %>UseCasePort(Protocol):
    """Use case port for <%= featureNamePascal %>."""
    
    def execute(self, command: <%= featureNamePascal %>Command) -> <%= featureNamePascal %>Result:
        """Execute <%= featureNamePascal %> use case."""
        ...


@dataclass
class <%= featureNamePascal %>UseCase:
    """Use case implementation for <%= featureNamePascal %>."""
    
    # Inject repository port here
    # repository: <%= featureNamePascal %>RepositoryPort
    
    def execute(self, command: <%= featureNamePascal %>Command) -> <%= featureNamePascal %>Result:
        """
        Execute <%= featureNamePascal %> use case.
        
        Args:
            command: Command containing input data
            
        Returns:
            Result containing output data
        """
        # Implement business logic here
        # Example:
        # entity = <%= featureNamePascal %>Entity.create_new()
        # entity = self.repository.save(entity)
        # return <%= featureNamePascal %>Result.from_entity(entity)
        
        raise NotImplementedError("Implement business logic")
