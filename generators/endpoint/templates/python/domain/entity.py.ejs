"""
Domain entity for <%= featureNamePascal %>.
Pure domain model - no framework imports (no SQLAlchemy, no FastAPI).
"""
from dataclasses import dataclass, field
from datetime import datetime
from uuid import UUID, uuid4


@dataclass
class <%= featureNamePascal %>Entity:
    """Domain entity for <%= featureNamePascal %>."""
    
    id: UUID = field(default_factory=uuid4)
    created_at: datetime = field(default_factory=datetime.utcnow)
    updated_at: datetime = field(default_factory=datetime.utcnow)
    
    # Add domain-specific fields here
    # name: str
    # amount: Decimal
    
    def __post_init__(self):
        if self.updated_at is None:
            self.updated_at = self.created_at
    
    @classmethod
    def create_new(cls) -> "<%= featureNamePascal %>Entity":
        """Create a new <%= featureNamePascal %> entity."""
        return cls(id=uuid4(), created_at=datetime.utcnow())
    
    def mark_updated(self) -> None:
        """Mark entity as updated."""
        self.updated_at = datetime.utcnow()
    
    # Add domain methods here (business logic)
    # Example:
    # def update_name(self, new_name: str) -> None:
    #     if not new_name or not new_name.strip():
    #         raise DomainException("Name cannot be empty")
    #     self.name = new_name
    #     self.mark_updated()
    
    def __eq__(self, other: object) -> bool:
        if not isinstance(other, <%= featureNamePascal %>Entity):
            return False
        return self.id == other.id
    
    def __hash__(self) -> int:
        return hash(self.id)
